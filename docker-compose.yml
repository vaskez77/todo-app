services:
  db:
    image: postgres:18-alpine
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  backend:
    build: .
    env_file:
      - ./.env
    ports:
      - "3000:3000"
    depends_on: 
      - db
  
  
